# ⚡ Быстрое Решение: Изменения Не Видны на Сервере

## 🎯 Проблема
Я загрузил изменения на сервер, но не вижу обновлений в приложении.

---

## ✅ Быстрая Проверка (5 шагов)

### 1️⃣ Проверить GitHub
```bash
# Зайдите на: https://github.com/alex-web13-2001/Managertaskfin1
# Убедитесь, что ваши последние коммиты там есть
```

### 2️⃣ Обновить код на сервере
```bash
cd /var/www/taskmanager
git pull origin main
```

### 3️⃣ Пересобрать фронтенд
```bash
npm run build
```

### 4️⃣ Перезапустить бэкенд
```bash
pm2 restart taskmanager-api
```

### 5️⃣ Очистить кэш браузера
```
Ctrl + Shift + R (Windows/Linux)
Cmd + Shift + R (Mac)
Или откройте в режиме инкогнито
```

---

## 🔴 Проблема: Git Pull Не Работает

### Ошибка: "Already up to date" но изменений нет

```bash
# Проверьте на какой ветке вы находитесь
git branch

# Если не на 'main', переключитесь:
git checkout main
git pull origin main
```

### Ошибка: "Permission denied"

```bash
# Проверьте права доступа
ls -la

# Исправьте владельца (замените 'youruser' на ваше имя пользователя)
sudo chown -R youruser:youruser /var/www/taskmanager
```

### Ошибка: Конфликт файлов

```bash
# Если есть локальные изменения, которые конфликтуют
git status

# Сохраните их или отмените
git stash  # Сохранить на потом
# или
git reset --hard origin/main  # Отменить локальные изменения (ОСТОРОЖНО!)
```

---

## 🔴 Проблема: Фронтенд Не Обновляется

### Build завершился, но изменений нет

```bash
# Проверьте что dist обновился
ls -lt dist/ | head -5

# Если файлы старые, удалите dist и соберите заново
rm -rf dist/
npm run build

# Перезагрузите nginx
sudo systemctl reload nginx
```

### В браузере всё ещё старая версия

```bash
# 1. Жёсткая перезагрузка: Ctrl+Shift+R
# 2. Или очистите кэш браузера полностью
# 3. Или откройте в режиме инкогнито
# 4. Или попробуйте с другого устройства/браузера
```

---

## 🔴 Проблема: Бэкенд Не Работает

### Сервер не запускается

```bash
# Проверьте логи
pm2 logs taskmanager-api

# Проверьте статус
pm2 status

# Если process не найден
pm2 start ecosystem.config.js

# Если порт занят
sudo lsof -i :3001
# Убейте процесс если нужно
sudo kill -9 <PID>
```

### Изменения в коде но API отвечает по-старому

```bash
# Убедитесь что сервер перезапустился
pm2 restart taskmanager-api

# Проверьте время запуска (должно быть недавнее)
pm2 status

# Проверьте что код обновился
git log --oneline -1
```

---

## 🔴 Проблема: База Данных

### Ошибки связанные с Prisma

```bash
# Генерируйте Prisma client
npx prisma generate

# Примените миграции
npx prisma migrate deploy

# Проверьте статус миграций
npx prisma migrate status

# Перезапустите сервер
pm2 restart taskmanager-api
```

---

## 📋 Полная Последовательность Действий

Используйте эту последовательность КАЖДЫЙ РАЗ при деплое:

```bash
# === НА ВАШЕМ ЛОКАЛЬНОМ КОМПЬЮТЕРЕ ===

# 1. Закоммитьте изменения
git add .
git commit -m "Описание изменений"

# 2. Загрузите на GitHub
git push origin main

# 3. Проверьте на GitHub
# https://github.com/alex-web13-2001/Managertaskfin1

# === НА СЕРВЕРЕ ===

# 4. Перейдите в директорию
cd /var/www/taskmanager

# 5. Скачайте изменения
git pull origin main

# 6. Установите зависимости (если изменился package.json)
npm install --production

# 7. Миграции БД (если изменилась схема)
npx prisma generate
npx prisma migrate deploy

# 8. Соберите фронтенд
npm run build

# 9. Перезапустите бэкенд
pm2 restart taskmanager-api

# 10. Проверьте логи
pm2 logs taskmanager-api --lines 20

# === В БРАУЗЕРЕ ===

# 11. Жёсткая перезагрузка
# Ctrl+Shift+R или Cmd+Shift+R
```

---

## 🆘 Экстренное Решение

Если НИЧЕГО не помогает, выполните полный сброс:

```bash
# НА СЕРВЕРЕ - ОСТОРОЖНО! Потеряете локальные изменения!

cd /var/www/taskmanager

# 1. Сохраните .env файл
cp .env .env.backup

# 2. Сбросьте репозиторий
git fetch origin
git reset --hard origin/main
git clean -fd

# 3. Восстановите .env
cp .env.backup .env

# 4. Переустановите всё
npm install --production
npx prisma generate
npx prisma migrate deploy
npm run build

# 5. Перезапустите
pm2 restart all

# 6. Проверьте
pm2 logs taskmanager-api
curl http://localhost:3001/health
```

---

## 🎓 Понимание Проблемы

### Откуда изменения должны попасть на сервер?

```
Ваш компьютер → GitHub → Сервер → Браузер
     (1)          (2)       (3)       (4)

1. git commit + git push
2. Хранятся на GitHub
3. git pull + npm run build + pm2 restart
4. Ctrl+Shift+R (очистить кэш)
```

### Почему изменения могут не быть видны?

1. ❌ Забыли сделать `git push` → изменения только локально
2. ❌ Забыли сделать `git pull` на сервере → сервер не знает об изменениях
3. ❌ Забыли `npm run build` → фронтенд не пересобран
4. ❌ Забыли `pm2 restart` → бэкенд работает со старым кодом
5. ❌ Кэш браузера → браузер показывает старую версию

---

## 📞 Проверка Перед Обращением за Помощью

Прежде чем просить помощь, соберите эту информацию:

```bash
# 1. Версия на GitHub
git ls-remote origin HEAD

# 2. Версия на сервере
cd /var/www/taskmanager
git log --oneline -1

# 3. Статус процессов
pm2 status

# 4. Последние логи
pm2 logs taskmanager-api --lines 50

# 5. Nginx логи
sudo tail -20 /var/log/nginx/taskmanager_error.log

# 6. Какие именно изменения не видны?
# Опишите конкретно
```

---

## ✅ Контрольный Список

Перед тем как сказать "не работает", убедитесь:

- [ ] Изменения закоммичены: `git status` показывает "clean"
- [ ] Изменения на GitHub: проверено визуально на сайте
- [ ] Код обновлён на сервере: `git pull origin main` выполнен
- [ ] Зависимости установлены: `npm install --production` выполнен (если нужно)
- [ ] Миграции применены: `npx prisma migrate deploy` выполнен (если нужно)
- [ ] Фронтенд собран: `npm run build` выполнен
- [ ] Бэкенд перезапущен: `pm2 restart taskmanager-api` выполнен
- [ ] Логи проверены: нет ошибок в `pm2 logs`
- [ ] Браузер обновлён: Ctrl+Shift+R нажато
- [ ] Проверено в инкогнито: тестировано в приватном режиме

---

## 🎯 Самые Частые Ошибки

### #1: Не сделал git push
```bash
# Решение
git push origin main
```

### #2: Не сделал git pull на сервере
```bash
# Решение
cd /var/www/taskmanager && git pull origin main
```

### #3: Не пересобрал фронтенд
```bash
# Решение
npm run build
```

### #4: Не перезапустил бэкенд
```bash
# Решение
pm2 restart taskmanager-api
```

### #5: Кэш браузера
```bash
# Решение
Ctrl + Shift + R
```

---

## 📚 Дополнительные Ресурсы

- [Полное Руководство по Деплою (English)](PRODUCTION_DEPLOYMENT.md)
- [Руководство по Проверке Обновлений (English)](SERVER_UPDATE_VISIBILITY_GUIDE.md)
- [README](README.md)

---

**Помните**: commit → push → pull → build → restart → refresh

**В 90% случаев проблема в одном из этих 6 шагов!**
