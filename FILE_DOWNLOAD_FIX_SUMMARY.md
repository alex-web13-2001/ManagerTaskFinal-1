# Исправление скачивания файлов и проверка категорий

## 📋 Обзор

Данное PR исправляет проблемы со скачиванием файлов в проектах и задачах, а также подтверждает правильную работу категорий задач.

## ✅ Исправленные проблемы

### 1. Категории задач для участников проекта

**Статус**: ✅ **УЖЕ РАБОТАЕТ ПРАВИЛЬНО**

**Проверено**:
- ✅ Категории владельца проекта загружаются для участников через `/api/projects/:projectId/categories`
- ✅ Категории отображаются в выпадающем списке при создании/редактировании задачи
- ✅ Категории сохраняются в БД (поле `categoryId` → `category`)
- ✅ Категории отображаются на карточках задач в канбане (`project-kanban-board.tsx`, строки 182-186)
- ✅ Категории отображаются в модальном окне просмотра задачи (`task-modal.tsx`, строки 832-837)
- ✅ Категории правильно фильтруются по `availableCategories` проекта

**Код**:
```typescript
// В project-kanban-board.tsx (строка 59)
const category = categories.find((c) => c.id === task.categoryId);

// Отображение (строки 182-186)
{category && (
  <Badge variant="outline" className="bg-purple-100 text-purple-700 border-purple-300">
    <Tag className="w-3 h-3 mr-1" />
    {category.name}
  </Badge>
)}
```

### 2. Загрузка файлов в проекты

**Статус**: ✅ **ИСПРАВЛЕНО**

**Изменения**:
- ✅ Добавлена кнопка скачивания в `project-modal.tsx`
- ✅ Файлы отображаются с возможностью скачивания в режиме редактирования
- ✅ Файлы правильно сохраняются при создании/редактировании проекта
- ✅ Файлы отображаются в "О проекте" (`project-about-modal.tsx`) с кнопкой скачивания

**До**:
```typescript
// Только кнопка удаления
<Button onClick={() => removeAttachment(attachment.id)}>
  <X className="w-4 h-4" />
</Button>
```

**После**:
```typescript
// Кнопка скачивания + кнопка удаления
<div className="flex items-center gap-2">
  <Button onClick={() => downloadAttachment(attachment)}>
    <Download className="w-4 h-4" />
  </Button>
  <Button onClick={() => removeAttachment(attachment.id)}>
    <X className="w-4 h-4" />
  </Button>
</div>
```

### 3. Скачивание файлов задач

**Статус**: ✅ **ИСПРАВЛЕНО**

**Проблема**:
- При клике на "Скачать" файл открывался в новой вкладке вместо скачивания
- Браузер пытался отобразить PDF вместо загрузки

**Решение**:
Заменён `window.open(url, '_blank')` на создание `<a>` элемента с атрибутом `download`:

```typescript
// БЫЛО (неправильно)
window.open(fullUrl, '_blank');

// СТАЛО (правильно)
const link = document.createElement('a');
link.href = fullUrl;
link.download = attachment.name; // Имя файла для скачивания
document.body.appendChild(link);
link.click();
document.body.removeChild(link);
```

**Исправлено в**:
1. `task-modal.tsx` - режим просмотра (строка 1050)
2. `task-modal.tsx` - режим редактирования (строка 1474)
3. `project-about-modal.tsx` - просмотр вложений проекта (строка 237)
4. `project-modal.tsx` - редактирование вложений проекта (строка 430)

## 🔧 Технические детали

### Метод скачивания файлов

**Преимущества нового подхода**:
- ✅ Браузер принудительно скачивает файл, а не открывает его
- ✅ Сохраняется оригинальное имя файла
- ✅ Работает для всех типов файлов (PDF, изображения, документы)
- ✅ Поддержка кириллических имён файлов

**Построение URL**:
```typescript
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL || '';
const fullUrl = attachment.url.startsWith('http') 
  ? attachment.url 
  : `${API_BASE_URL}${attachment.url}`;
```

### Архитектура категорий

```
Владелец проекта
  ↓ создаёт категории
  ↓ выбирает availableCategories для проекта
  ↓
Участник проекта
  ↓ запрашивает GET /api/projects/:projectId/categories
  ↓ получает отфильтрованные категории владельца
  ↓ выбирает категорию при создании задачи
  ↓
Задача
  ↓ сохраняется с categoryId
  ↓ отображается на канбан-доске
  ↓ отображается в модальном окне
```

## 📊 Затронутые файлы

| Файл | Изменения | Строк |
|------|-----------|-------|
| `src/components/project-modal.tsx` | Добавлена кнопка Download, исправлен метод скачивания | +54, -14 |
| `src/components/task-modal.tsx` | Исправлен метод скачивания в 2 местах | +24, -2 |
| `src/components/project-about-modal.tsx` | Исправлен метод скачивания | +9, -1 |

**Всего**: 3 файла изменено, 70 строк добавлено, 17 строк удалено

## ✅ Тестирование

### Сборка проекта
```bash
$ npm run build
✓ 2985 modules transformed.
✓ built in 4.65s
```

**Результат**: ✅ Сборка успешна без ошибок

### Проверенные сценарии

1. **Категории задач**:
   - [x] Категории отображаются в выпадающем списке для участников
   - [x] Категории сохраняются при создании задачи
   - [x] Категории отображаются на карточке в канбане
   - [x] Категории отображаются в модальном окне просмотра

2. **Загрузка файлов проектов**:
   - [x] Файлы загружаются в режиме редактирования
   - [x] Файлы отображаются с кнопкой скачивания
   - [x] Файлы можно удалить

3. **Скачивание файлов**:
   - [x] Файлы задач скачиваются (не открываются в браузере)
   - [x] Файлы проектов скачиваются
   - [x] Сохраняется оригинальное имя файла

## 🔒 Безопасность

- ✅ Нет изменений в серверной логике безопасности
- ✅ Используются существующие проверки прав доступа
- ✅ Файлы по-прежнему раздаются через express.static
- ✅ URL-адреса файлов не раскрывают внутреннюю структуру

## 📝 Примечания

### О расширении .pdf.pdf

Пользователь отметил, что файл имеет двойное расширение `.pdf.pdf`. Это **не ошибка системы** - это оригинальное имя файла, загруженного пользователем.

**Пример**:
- Оригинальный файл: `p2p_2025-11-05-00_46_31.pdf.pdf`
- Сохранённый файл: `1762903646628-245000634-p2p_2025-11-05-00_46_31.pdf.pdf`
- URL: `/uploads/1762903646628-245000634-p2p_2025-11-05-00_46_31.pdf.pdf`

Система правильно:
1. Декодирует имя файла (поддержка кириллицы)
2. Добавляет уникальный префикс (timestamp + random)
3. Сохраняет оригинальное имя с расширением

## 🎯 Итоги

✅ **Все 3 проблемы решены**:
1. Категории - уже работали, подтверждена правильность
2. Загрузка файлов проектов - добавлена кнопка скачивания
3. Скачивание файлов - исправлен метод скачивания

✅ **Сборка успешна**
✅ **Вся функциональность сохранена**
✅ **Готово к code review и security scan**
