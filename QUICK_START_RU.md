# Quick Start - После Применения Исправлений

## Шаг 1: Обновите код

```bash
git pull origin copilot/fix-create-project-error
```

## Шаг 2: Установите зависимости

```bash
npm install
```

## Шаг 3: Примените миграцию базы данных

**ВАЖНО**: Это обязательный шаг!

```bash
npm run prisma:migrate
```

Когда появится запрос на имя миграции, введите: `fix_project_links`

Затем сгенерируйте клиент Prisma:

```bash
npm run prisma:generate
```

## Шаг 4: Соберите фронтенд

```bash
npm run build
```

## Шаг 5: Перезапустите сервер

Если используете PM2:
```bash
pm2 restart all
```

Или просто перезапустите:
```bash
npm run dev:server
```

## Шаг 6: Проверьте исправления

### ✅ Проверка #1: Создание проекта
1. Откройте приложение
2. Нажмите "Создать проект"
3. Заполните название и описание
4. Добавьте ссылки (опционально)
5. Нажмите "Создать"
6. **Ожидается**: Проект создан успешно, без ошибок

### ✅ Проверка #2: Загрузка нескольких файлов
1. Откройте любую задачу или создайте новую
2. В разделе "Файлы" нажмите на область загрузки
3. Выберите несколько файлов (Ctrl+Click или Cmd+Click)
4. **Ожидается**: Все файлы загружаются одновременно, показывается прогресс

### ✅ Проверка #3: Перетаскивание новой задачи
1. Откройте любую Канбан-доску (Личные задачи или Проект)
2. Создайте новую задачу
3. **Ожидается**: Задача появляется мгновенно в правильной колонке
4. Попробуйте перетащить задачу в другую колонку
5. **Ожидается**: Задача перетаскивается без необходимости обновления страницы

### ✅ Проверка #4: Ссылки проекта
1. Создайте или отредактируйте проект
2. Добавьте несколько ссылок (название + URL)
3. Сохраните проект
4. **Ожидается**: Ссылки сохраняются и отображаются корректно

### ✅ Проверка #5: Файлы проекта
1. Отредактируйте проект
2. Загрузите файлы в разделе "Файлы"
3. Сохраните проект
4. **Ожидается**: Файлы отображаются в деталях проекта

## Возможные проблемы

### Проблема: "Error: Не удалось создать проект"
**Решение**: Убедитесь, что миграция базы данных применена (Шаг 3)

### Проблема: Миграция не применяется
**Решение**: Проверьте подключение к базе данных в `.env` файле

### Проблема: Файлы не загружаются
**Решение**: Проверьте, что папка `uploads/` существует и имеет права на запись

## Логи для отладки

Если что-то не работает, проверьте логи сервера:

```bash
# Если используете PM2
pm2 logs

# Или запустите сервер напрямую для просмотра логов
npm run dev:server
```

В логах ищите:
- `📎 Uploading X file(s)` - загрузка файлов
- `✅ Task created` - создание задачи
- `Create project error` - ошибки создания проекта

## Документация

Подробная информация обо всех исправлениях: `FIXES_DETAILED.md`

Инструкции по миграции: `MIGRATION_FIX_PROJECT_LINKS.md`

## Помощь

Если проблемы сохраняются:
1. Проверьте, что все шаги выполнены
2. Очистите кэш браузера (Ctrl+F5)
3. Проверьте консоль браузера на ошибки (F12)
4. Проверьте логи сервера
5. Убедитесь, что база данных работает

## Успех! 🎉

Если все проверки прошли успешно, все 5 критических проблем исправлены!
