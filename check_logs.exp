#!/usr/bin/expect -f

set timeout 30
set host "82.97.253.158"
set user "root"
set password "frGyWb_kF#651v"

spawn ssh $user@$host
expect {
    "yes/no" { send "yes\r"; exp_continue }
    "assword:" { send "$password\r" }
}

expect "#"
send "node -v\r"
expect "#"
send "pm2 logs kanban-server --lines 50 --nostream\r"
expect "#"
send "exit\r"
expect eof
