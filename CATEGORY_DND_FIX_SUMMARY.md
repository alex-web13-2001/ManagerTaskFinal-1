# Исправление категорий и drag-and-drop для участников проекта

## 📋 Проблемы

### 1. Категории не отображаются для участников проекта
**Описание**: Участники проекта видят категории в выпадающем списке, могут их выбрать, но они не отображаются на карточках задач и не сохраняются.

**Корневая причина**: 
Kanban board использовал `categories` из `useApp()`, которые содержат **категории текущего пользователя**, а не категории владельца проекта. Когда участник создавал задачу с категорией владельца, `categoryId` сохранялся в БД, но при отображении на kanban board категория не находилась, так как искалась в неправильном массиве.

```typescript
// БЫЛО (неправильно):
const { teamMembers, categories, setIsDragging } = useApp();
const category = categories.find((c) => c.id === task.categoryId);
// categories - это категории ТЕКУЩЕГО пользователя
```

### 2. Вновь созданные задачи нельзя перетаскивать
**Описание**: После создания задачи она появляется на канбане, но её нельзя перетащить без обновления страницы.

**Корневая причина**:
Анимация появления задачи начиналась с `opacity: 0, scale: 0.95`, что мешало инициализации drag-and-drop. Элемент не был полностью "готов" для перетаскивания пока анимация не завершалась.

```typescript
// БЫЛО (неправильно):
initial={isInitialRender ? { opacity: 1 } : { opacity: 0, scale: 0.95 }}
// Новые задачи (isInitialRender=false) начинались с opacity: 0
```

## ✅ Решение

### Исправление категорий

**Изменения в `project-kanban-board.tsx`**:

1. **Добавлен state для категорий проекта**:
```typescript
const [projectCategories, setProjectCategories] = React.useState<any[]>([]);
```

2. **Добавлена загрузка категорий проекта**:
```typescript
React.useEffect(() => {
  const loadProjectCategories = async () => {
    try {
      const { projectsAPI } = await import('../utils/api-client');
      const cats = await projectsAPI.getProjectCategories(projectId);
      setProjectCategories(cats);
    } catch (error) {
      console.error('Failed to load project categories for kanban:', error);
      setProjectCategories([]);
    }
  };
  
  if (projectId) {
    loadProjectCategories();
  }
}, [projectId]);
```

3. **Передача категорий через props**:
```typescript
// В DraggableTaskCard
const availableCategories = projectCategories.length > 0 ? projectCategories : categories;
const category = availableCategories.find((c) => c.id === task.categoryId);
```

4. **Обновлены типы и мемоизация**:
- Добавлен prop `projectCategories?: any[]` в `DraggableTaskCard`
- Добавлен prop `projectCategories?: any[]` в `DroppableColumn`
- Обновлены `React.memo` сравнения для включения `projectCategories`
- Добавлена проверка `categoryId` в мемоизацию `MemoizedDraggableTaskCard`

### Исправление drag-and-drop

**Изменение в `project-kanban-board.tsx`**:

```typescript
// БЫЛО:
initial={isInitialRender ? { opacity: 1 } : { opacity: 0, scale: 0.95 }}

// СТАЛО:
initial={isInitialRender ? { opacity: 1 } : { opacity: 1, scale: 1 }}
```

Теперь новые задачи сразу появляются с полной непрозрачностью и нормальным масштабом, что позволяет drag-and-drop работать немедленно.

## 📊 Поток данных

### Категории для участников проекта

```
1. Владелец проекта
   ↓ создаёт категории (например: "Дизайн", "Backend", "Frontend")
   ↓ выбирает availableCategories для проекта
   ↓
2. Участник открывает проект
   ↓ ProjectKanbanBoard загружается
   ↓ useEffect вызывает getProjectCategories(projectId)
   ↓
3. API /api/projects/:projectId/categories
   ↓ возвращает категории ВЛАДЕЛЬЦА, отфильтрованные по availableCategories
   ↓
4. Категории сохраняются в state projectCategories
   ↓
5. Категории передаются в DraggableTaskCard
   ↓
6. При отображении задачи:
   const category = projectCategories.find((c) => c.id === task.categoryId)
   ↓
7. Категория отображается на карточке задачи
```

### Создание задачи с категорией

```
1. Участник создаёт задачу
   ↓ выбирает категорию из projectCategories
   ↓
2. TaskModal сохраняет taskData с categoryId
   ↓
3. POST /api/tasks { categoryId: "design" }
   ↓
4. Сервер сохраняет в БД: category = "design"
   ↓
5. Сервер возвращает task с categoryId: "design"
   ↓
6. Task добавляется в state
   ↓
7. ProjectKanbanBoard отображает задачу
   ↓ находит категорию в projectCategories
   ↓ отображает бейдж с категорией
```

## 🧪 Тестирование

### Сценарий 1: Категории для участника

1. ✅ Владелец создаёт проект и категории
2. ✅ Владелец приглашает участника
3. ✅ Участник создаёт задачу и выбирает категорию владельца
4. ✅ Задача сохраняется с categoryId
5. ✅ Категория отображается на карточке задачи в канбане
6. ✅ Категория доступна в фильтрах

### Сценарий 2: Drag-and-drop новых задач

1. ✅ Создать новую задачу
2. ✅ Задача появляется на канбане
3. ✅ Задачу можно сразу перетащить в другую колонку
4. ✅ Не требуется обновление страницы

### Сборка

```bash
$ npm run build
✓ 2985 modules transformed.
✓ built in 4.64s
```

**Результат**: ✅ Успешно без ошибок

### Безопасность

```
CodeQL scan: 0 alerts
```

**Результат**: ✅ Уязвимостей не найдено

## 📝 Измененные файлы

### src/components/project-kanban-board.tsx

**Добавлено** (+35 строк):
- State для `projectCategories`
- useEffect для загрузки категорий проекта
- Prop `projectCategories` в `DraggableTaskCard`
- Prop `projectCategories` в `DroppableColumn`
- Логика выбора категорий: `projectCategories` или `categories`
- Обновлены React.memo сравнения

**Изменено** (-4 строки):
- Анимация `initial` для новых задач
- Мемоизация компонентов

**Всего**: 1 файл изменён, 35 строк добавлено, 4 строки удалено

## 🎯 Результаты

### До исправления

❌ Категории не отображаются на карточках задач для участников  
❌ Вновь созданные задачи нельзя перетаскивать без обновления страницы  
❌ Категории загружаются только для текущего пользователя  
❌ Фильтры по категориям не работают для участников  

### После исправления

✅ Категории владельца загружаются для участников проекта  
✅ Категории отображаются на карточках задач  
✅ Категории сохраняются при создании/редактировании задач  
✅ Категории доступны в фильтрах канбана  
✅ Вновь созданные задачи можно сразу перетаскивать  
✅ Не требуется обновление страницы после создания задачи  
✅ Drag-and-drop работает мгновенно  

## 🔒 Безопасность

- ✅ CodeQL: 0 уязвимостей
- ✅ Нет изменений в серверной логике безопасности
- ✅ Используются существующие проверки прав доступа
- ✅ API endpoint `/api/projects/:projectId/categories` уже защищён

## 💡 Ключевые моменты

1. **Категории принадлежат владельцу проекта**, не участникам
2. **Участники видят категории владельца**, отфильтрованные по `availableCategories`
3. **ProjectKanbanBoard теперь загружает категории проекта** для правильного отображения
4. **Анимация не должна мешать drag-and-drop** - используем `opacity: 1` для новых задач
5. **Мемоизация обновлена** для включения `projectCategories` и `categoryId`

## ✅ Заключение

Обе проблемы решены:

1. ✅ **Категории для участников проекта работают полностью**
   - Загрузка категорий владельца
   - Отображение на карточках задач
   - Сохранение при создании/редактировании
   - Доступность в фильтрах

2. ✅ **Drag-and-drop работает для новых задач**
   - Нет мешающей анимации
   - Задачи можно перетаскивать сразу
   - Не требуется обновление страницы

**Commit**: d9ec0c0  
**Статус**: Готово к мёрджу ✅
